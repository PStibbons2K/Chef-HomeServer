[libdefaults]
  default_realm = ${KRB5_REALMNAME}

[realms]
  ${KRB5_REALMNAME} = {
    kdc = ${SERVER_NAME}.${DNS_REALMNAME}
    admin_server = ${SERVER_NAME}.${DNS_REALMNAME}
  }

[domain_realm]
  ${DNS_REALMNAME} = ${KRB5_REALMNAME}
  .${DNS_REALMNAME} = ${KRB5_REALMNAME}